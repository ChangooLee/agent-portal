# Data Cloud 커넥터 구현 계획

## 🎯 구현 상태: 95% 완료

**마지막 업데이트**: 2025-11-27

| Phase | 상태 | 완료일 | 비고 |
|-------|------|--------|------|
| Phase 0: 사전 준비 | ✅ 완료 | 2025-11-27 | Git 커밋 완료 |
| Phase 1: DB 스키마 및 Backend API | ✅ 완료 | 2025-11-27 | 서비스/라우터 구현 |
| Phase 2: Admin UI | ✅ 완료 | 2025-11-27 | 7개 컴포넌트 모두 구현 |
| Phase 3: 테스트 대상 DB | ✅ 완료 | 2025-11-27 | 3/3 실행 중인 DB 연결 완료 |
| Phase 4: Kong Gateway 통합 | ⚠️ 코드 구현 | 2025-11-27 | 코드는 있으나 테스트 필요 |
| Phase 5: 브라우저 테스트 | ✅ 완료 | 2025-11-27 | 전체 기능 테스트 완료 |
| Phase 6: 문서화 | ✅ 완료 | 2025-11-27 | 문서 업데이트 완료 |

### 구현 완료 기능 (Backend)

- ✅ DB 연결 CRUD (생성/조회/수정/삭제)
- ✅ 연결 테스트 (health check)
- ✅ SQLAlchemy 스키마 리플렉션
- ✅ 실시간 SQL 쿼리 실행 (Zero Copy)
- ✅ 비밀번호 Fernet 암호화
- ✅ 스키마 캐싱
- ✅ 비즈니스 용어집 API (`/datacloud/connections/{id}/terms`)
- ✅ 권한 관리 API (`/datacloud/connections/{id}/permissions`)
- ✅ Kong Gateway 통합 코드 (datacloud_service.py)
- ✅ Text-to-SQL API (`/datacloud/connections/{id}/generate-sql`)
- ✅ Gateway Overview에 Data Cloud 정보 포함

### 구현 완료 기능 (Frontend)

- ✅ 연결 목록 테이블 (Hero 섹션 포함)
- ✅ 연결 추가/수정 모달
- ✅ 스키마 뷰어 모달 (테이블/컬럼 트리) + 닫기 버튼 개선
- ✅ 쿼리 에뮬레이터 모달 (SQL 에디터 + 결과 테이블) + 닫기 버튼 개선
- ✅ Text-to-SQL 섹션 (자연어 → SQL 생성)
- ✅ 비즈니스 용어집 모달 (용어 추가/삭제)
- ✅ 권한 관리 모달 (사용자/그룹 권한 관리)
- ✅ 상단 네비게이션 메뉴 추가 ("Data Cloud")
- ✅ MCP 스타일 Glassmorphism 적용
- ✅ Gateway UI에 Data Cloud 섹션 표시

### ⏳ 미구현/향후 작업

| 계획된 기능 | 상태 | 비고 |
|-------------|------|------|
| 차트 시각화 | ⏳ 향후 | 쿼리 결과 → 차트 |
| Kong Gateway 실제 테스트 | ⏳ 향후 | 코드는 있음 |
| 추가 DB 연결 | ⏳ 향후 | docker 미실행 서비스들 |

### 연결된 데이터베이스

| DB | 호스트 | 상태 | 비고 |
|-----|--------|------|------|
| Agent Portal MariaDB | mariadb:3306 | ✅ 정상 | 스키마/쿼리 테스트 완료 |
| LiteLLM PostgreSQL | litellm-postgres:5432 | ✅ 정상 | 스키마/쿼리 테스트 완료 |
| Monitoring ClickHouse | monitoring-clickhouse:8123 | ✅ 정상 | 스키마/쿼리 테스트 완료 |
| Langfuse PostgreSQL | langfuse-db:5432 | ❌ 미연결 | docker 미실행 |
| Helicone PostgreSQL | helicone-db:5432 | ❌ 미연결 | docker 미실행 |
| Kong PostgreSQL | kong-db:5432 | ❌ 미연결 | docker 미실행 |
| Konga PostgreSQL | konga-db:5432 | ❌ 미연결 | docker 미실행 |

### 우선순위별 남은 작업

**P0 (핵심 기능)**:
- ⏳ 비즈니스 용어집 UI 구현
- ⏳ 권한 관리 UI 구현
- ⏳ Kong Gateway 통합 테스트

**P1 (향상 기능)**:
- ⏳ 차트 시각화 (쿼리 결과 → 차트)
- ⏳ 모달 닫기 버튼 가시성 개선 ("닫기" 텍스트 추가)
- ⏳ 추가 DB 서비스 시작 시 자동 등록

**P2 (향후 작업)**:
- ⏳ Identity Resolution (여러 DB 간 엔티티 매핑)
- ⏳ SSL/TLS, SSH 터널 지원

---

## 현재 상태

기존 계획 문서 위치:

- [`README.md`](README.md) Stage 5: 데이터베이스 커넥터 (Data-Cloud 스타일)
- [`README.md`](README.md) 섹션 3.4: Data-Cloud 스타일 커넥터

---

## Salesforce Data Cloud 기능 분석 (리서치 결과)

### 모방할 핵심 기능

| Salesforce 기능 | 우리 구현 | 우선순위 |

|----------------|----------|----------|

| **Zero Copy Integration** | DB 직접 연결, 데이터 복제 없이 실시간 쿼리 | P0 |

| **스키마 카탈로그** | 테이블/컬럼/PK/FK/코멘트 자동 수집 | P0 |

| **비즈니스 용어집** | 기술명 ↔ 비즈니스명 매핑 | P1 |

| **200+ 커넥터** | MariaDB, PostgreSQL, Oracle, ClickHouse 등 | P0 |

| **Identity Resolution** | (향후) 여러 DB 간 엔티티 매핑 | P2 |

| **데이터 변환** | SQL 쿼리 결과 → 표/차트 변환 | P1 |

| **정책 기반 거버넌스** | 권한 관리 (사용자/그룹별 접근 제어) | P0 |

### 활용 가능한 오픈소스 (라이선스 검증 완료)

| 프로젝트 | 라이선스 | 활용 기능 |

|----------|----------|----------|

| **SQLAlchemy** | MIT | DB 연결, 스키마 리플렉션, 메타데이터 수집 |

| **Airbyte** | MIT/ELv2 | 커넥터 패턴 참조 (300+ 커넥터) |

| **OpenMetadata** | Apache 2.0 | 데이터 카탈로그 UI 패턴 참조 |

| **DataHub** | Apache 2.0 | 메타데이터 수집 패턴 참조 |

| **Apache Superset** | Apache 2.0 | SQL 에디터 UI 패턴 참조 |

| **cryptography** | Apache 2.0/BSD | 비밀번호 암호화 |

### SQLAlchemy 스키마 리플렉션 활용

```python
from sqlalchemy import create_engine, MetaData, inspect

engine = create_engine(connection_url)
inspector = inspect(engine)

# 테이블 목록
tables = inspector.get_table_names()

# 컬럼 정보
columns = inspector.get_columns(table_name)

# PK/FK 정보
pk = inspector.get_pk_constraint(table_name)
fks = inspector.get_foreign_keys(table_name)

# 코멘트
table_comment = inspector.get_table_comment(table_name)
```

---

## 구현 목표

1. **Admin UI 신설**: `/admin/datacloud` - MCP 관리 화면과 유사한 인터페이스
2. **Zero Copy DB 커넥터**: 데이터 복제 없이 스키마/메타 읽기, 실시간 쿼리
3. **Kong Gateway 통합**: API Key 인증, Rate Limiting
4. **쿼리 에뮬레이터**: SQL 에디터 + 결과 테이블 + 차트 변환

## 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                     Data Cloud Architecture                      │
│                                                                 │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐  │
│  │  Admin UI    │─────▶│  Backend BFF │─────▶│    Kong      │  │
│  │  (DataCloud) │      │  (DB API)    │      │  Gateway     │  │
│  └──────────────┘      └──────────────┘      └──────┬───────┘  │
│                               │                      │          │
│                        ┌──────▼───────┐              │          │
│                        │ DB Connector │◀─────────────┘          │
│                        │   Service    │                         │
│                        │ (SQLAlchemy) │                         │
│                        └──────┬───────┘                         │
│         ┌──────────┬──────────┼──────────┬──────────┐          │
│         ▼          ▼          ▼          ▼          ▼          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────┐  │
│  │ MariaDB  │ │ LiteLLM  │ │ Langfuse │ │  Kong    │ │Click │  │
│  │  (3306)  │ │ Postgres │ │ Postgres │ │ Postgres │ │House │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Phase 0: 사전 준비 (Git 안전 보관)

```bash
git add -A
git commit -m "chore: Data Cloud 구현 전 현재 상태 보관"
git push origin main
```

---

## Phase 1: DB 스키마 및 Backend API

### 1.1 DB 스키마

파일: `scripts/init-datacloud-schema.sql` (신규)

테이블:

- `db_connections` - 연결 정보 (암호화된 비밀번호, Kong 연동)
- `db_schema_cache` - 스키마 메타데이터 캐시 (SQLAlchemy 리플렉션 결과)
- `db_business_terms` - 비즈니스 용어집
- `db_connection_permissions` - 권한 관리

### 1.2 Backend 서비스

파일: `backend/app/services/datacloud_service.py` (신규)

**SQLAlchemy 기반 구현**:

- `get_schema_metadata()` - 스키마 리플렉션으로 메타데이터 수집
- `execute_query()` - 실시간 쿼리 실행 (Zero Copy)
- `encrypt/decrypt_password()` - Fernet 암호화

### 1.3 Backend API 라우터

파일: `backend/app/routes/datacloud.py` (신규)

---

## Phase 2: Admin UI

파일: `webui/src/routes/(app)/admin/datacloud/+page.svelte` (신규)

### UI 컴포넌트

1. **Hero 섹션** - Stats (총 연결, 활성, 정상)
2. **연결 목록 테이블** - 이름, DB 타입, 호스트, 상태, 액션
3. **연결 추가/수정 모달** - DB 타입, 호스트, 포트, 인증 정보
4. **스키마 뷰어 모달** - 테이블/컬럼 트리 (OpenMetadata 패턴 참조)
5. **쿼리 에뮬레이터 모달** - SQL 에디터 (Superset 패턴 참조)
6. **비즈니스 용어집 모달** - 기술명 ↔ 비즈니스명 매핑
7. **권한 관리 모달** - 사용자/그룹 권한

---

## Phase 3: 테스트 대상 DB (프로젝트 내 모든 DB)

| DB | 호스트 | 포트 | 드라이버 |

|-----|--------|------|----------|

| mariadb | mariadb | 3306 | aiomysql |

| litellm-postgres | litellm-postgres | 5432 | asyncpg |

| helicone-db | helicone-db | 5432 | asyncpg |

| kong-db | kong-db | 5432 | asyncpg |

| konga-db | konga-db | 5432 | asyncpg |

| monitoring-clickhouse | monitoring-clickhouse | 8123 | clickhouse-connect |

**원격지 DB 지원**: SSL/TLS, SSH 터널, 타임아웃 설정

---

## Phase 4: Kong Gateway 통합

`kong_service.py` 재사용 - Service/Route/Consumer 자동 생성

---

## Phase 5: 브라우저 테스트

1. `/admin/datacloud` 페이지 접속
2. 7개 DB 연결 등록 및 테스트
3. 스키마 조회 및 메타데이터 확인
4. 쿼리 에뮬레이터로 SELECT 실행
5. 비즈니스 용어 매핑 테스트
6. 권한 관리 테스트

---

## Phase 6: 문서화 및 Git Push

1. `docs/plans/DATACLOUD_PLAN.md` 작성
2. `docs/CURRENT_STATUS.md` 업데이트
3. `README.md` Stage 5 상태 업데이트
4. Git 커밋 및 Push

---

## 수정 파일 목록

| 파일 | 작업 |

|------|------|

| `scripts/init-datacloud-schema.sql` | 신규 |

| `backend/app/services/datacloud_service.py` | 신규 |

| `backend/app/routes/datacloud.py` | 신규 |

| `backend/app/main.py` | 수정 |

| `backend/requirements.txt` | 수정 |

| `webui/src/routes/(app)/admin/datacloud/+page.svelte` | 신규 |

| `docs/plans/DATACLOUD_PLAN.md` | 신규 |

## 의존성 추가

```
sqlalchemy>=2.0        # MIT - 스키마 리플렉션
aiomysql               # MIT - MariaDB/MySQL async
asyncpg                # Apache 2.0 - PostgreSQL async
clickhouse-connect     # Apache 2.0 - ClickHouse
cryptography           # Apache 2.0/BSD - 비밀번호 암호화
```

## 예상 시간: 약 14시간