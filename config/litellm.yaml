# LiteLLM Configuration
# LLM Gateway 설정 - DB 기반 모델 관리

# OTEL Resource Attributes (project_id 주입)
environment_variables:
  OTEL_RESOURCE_ATTRIBUTES: "project_id=8c59e361-3727-418c-bc68-086b69f7598b"

# Model List: DB에서 관리 (LiteLLM_ProxyModelTable)
# UI에서 모델 추가/수정/삭제 가능

# General Settings
general_settings:
  # Enable DB-based model management (for /model/new, /model/update, /model/delete)
  store_model_in_db: true
  
  # OpenTelemetry Integration
  set_verbose: true
  otel: true

# OTEL Settings (ClickHouse 기반 모니터링)
litellm_settings:
  callbacks: ["otel"]
  success_callback: ["otel"]
  failure_callback: ["otel"]
  use_client: true

# Rate Limiting
router_settings:
  enable_pre_call_checks: true
  allowed_fails: 2

# Cost Tracking
cost_tracking:
  enabled: true


