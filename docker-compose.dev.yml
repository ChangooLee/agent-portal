# Development Override Docker Compose
# Extends docker-compose.yml with development-specific settings
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml build backend
#
# Shortcut (create alias in .bashrc/.zshrc):
#   alias dcdev='docker compose -f docker-compose.yml -f docker-compose.dev.yml'

services:
  # Backend - Development overrides
  backend:
    environment:
      - DEBUG=true
      - LOG_LEVEL=debug
    # Uncomment for live code reload:
    # volumes:
    #   - ./backend/app:/app/app

  # WebUI - Development overrides  
  webui:
    ports:
      - "3001:3001"    # Dev server port
    environment:
      - NODE_ENV=development
    # Uncomment for live code reload:
    # volumes:
    #   - ./webui/src:/app/src

  # LiteLLM - Development overrides
  litellm:
    environment:
      - LITELLM_LOG=DEBUG

# Development-only volumes (for live reload)
# volumes:
#   backend_code:
#     driver: local
#     driver_opts:
#       type: none
#       device: ${PWD}/backend/app
#       o: bind
