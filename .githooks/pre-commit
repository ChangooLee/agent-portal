#!/bin/bash
# Pre-commit hook: 문서 일관성 체크 및 린트

echo "🔍 Pre-commit checks..."

# 0. 민감 정보 체크 (CRITICAL - 최우선)
echo "  🔐 Checking for sensitive information..."

# 민감 파일 패턴
SENSITIVE_PATTERNS=".env.bak|.env.backup|.env.old|\.key$|\.pem$|_secret|_credentials"
SENSITIVE_FILES=$(git diff --cached --name-only | grep -E "$SENSITIVE_PATTERNS")

if [ -n "$SENSITIVE_FILES" ]; then
    echo ""
    echo "  ❌ ERROR: 민감 정보가 포함된 파일이 감지되었습니다!"
    echo "     다음 파일들은 커밋할 수 없습니다:"
    echo "$SENSITIVE_FILES" | sed 's/^/     - /'
    echo ""
    echo "     해결 방법:"
    echo "     1. git reset HEAD <파일명>  # 스테이지에서 제거"
    echo "     2. rm <파일명>              # 파일 삭제"
    echo "     3. .gitignore에 추가        # 향후 방지"
    echo ""
    exit 1
fi

# API 키 패턴 체크 (실제 키 값이 코드에 하드코딩된 경우)
API_KEY_PATTERNS="sk-or-v1-[a-zA-Z0-9]{64}|sk-[a-zA-Z0-9]{48}|sk-ant-[a-zA-Z0-9-]{80,}"
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM)

if [ -n "$STAGED_FILES" ]; then
    for file in $STAGED_FILES; do
        if [ -f "$file" ] && [ "$file" != ".gitignore" ]; then
            # 바이너리 파일 제외
            if file "$file" | grep -q "text"; then
                FOUND_KEYS=$(grep -E "$API_KEY_PATTERNS" "$file" 2>/dev/null || true)
                if [ -n "$FOUND_KEYS" ]; then
                    echo ""
                    echo "  ❌ ERROR: API 키가 파일에 하드코딩되어 있습니다!"
                    echo "     파일: $file"
                    echo ""
                    echo "     해결 방법:"
                    echo "     1. 환경 변수로 대체: os.environ.get('API_KEY')"
                    echo "     2. .env 파일 사용 (gitignore에 포함)"
                    echo ""
                    exit 1
                fi
            fi
        fi
    done
fi

echo "  ✅ 민감 정보 체크 완료"

# 1. UI 파일 변경 시 Skills 시스템 업데이트 확인
UI_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '^webui/src/(routes|lib/components)/.*\.(svelte|ts)$')

if [ -n "$UI_FILES" ]; then
    echo "  ⚠️  UI files changed. Consider running: ./scripts/update-ui-skills.sh"
    echo "     Changed files:"
    echo "$UI_FILES" | sed 's/^/     - /'
fi

# 2. .mdc 파일 lastUpdated 자동 업데이트
MDC_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '\.mdc$')

if [ -n "$MDC_FILES" ]; then
    echo "  📝 Updating lastUpdated in .mdc files..."
    
    TODAY=$(date +%Y-%m-%d)
    
    for file in $MDC_FILES; do
        if [ -f "$file" ]; then
            # lastUpdated 필드 업데이트
            if grep -q "^lastUpdated:" "$file"; then
                # macOS와 Linux 호환 sed 사용
                if [[ "$OSTYPE" == "darwin"* ]]; then
                    # macOS
                    sed -i '' "s/^lastUpdated: .*/lastUpdated: $TODAY/" "$file"
                else
                    # Linux
                    sed -i "s/^lastUpdated: .*/lastUpdated: $TODAY/" "$file"
                fi
                echo "     ✅ Updated $file (lastUpdated: $TODAY)"
                # 변경사항 stage에 추가
                git add "$file"
            else
                echo "     ⚠️  $file: lastUpdated field not found"
            fi
        fi
    done
fi

# 3. 핵심 문서 동기화 체크
CORE_DOCS=("CLAUDE.md" "AGENTS.md" "README.md" "DEVELOP.md")
DOCS_CHANGED=$(git diff --cached --name-only --diff-filter=ACM | grep -E '^(CLAUDE|AGENTS|README|DEVELOP)\.md$')

if [ -n "$DOCS_CHANGED" ]; then
    echo "  📝 Core documents changed:"
    echo "$DOCS_CHANGED" | sed 's/^/     - /'
    echo "     Ensure all core documents are synchronized."
fi

# 4. Python 파일 린트 (backend 변경 시)
PYTHON_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '\.py$')

if [ -n "$PYTHON_FILES" ]; then
    echo "  🐍 Linting Python files..."
    # flake8가 설치되어 있으면 실행
    if command -v flake8 &> /dev/null; then
        echo "$PYTHON_FILES" | xargs flake8 --max-line-length=100 || true
    fi
fi

# 5. Svelte 파일 체크 (webui 변경 시)
SVELTE_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '\.svelte$')

if [ -n "$SVELTE_FILES" ]; then
    echo "  🎨 Checking Svelte files..."
    # svelte-check가 설치되어 있으면 실행
    if command -v svelte-check &> /dev/null; then
        cd webui && svelte-check --threshold warning --fail-on-warnings || true
        cd ..
    fi
fi

# 6. 임시 문서 체크
TEMP_PATTERNS="IMPLEMENTATION_.*\.md|TEMP_.*\.md|TODO_.*\.md|DRAFT_.*\.md|WIP_.*\.md|DECISION_.*\.md|ANALYSIS_.*\.md|DEBUG_.*\.md|.*_TEMP\.md|.*_WIP\.md|.*_DRAFT\.md"
TEMP_DOCS=$(find . -maxdepth 3 -type f \( -name "IMPLEMENTATION_*.md" -o -name "TEMP_*.md" -o -name "TODO_*.md" -o -name "DRAFT_*.md" -o -name "WIP_*.md" -o -name "DECISION_*.md" -o -name "ANALYSIS_*.md" -o -name "DEBUG_*.md" -o -name "*_TEMP.md" -o -name "*_WIP.md" -o -name "*_DRAFT.md" \) 2>/dev/null | grep -v -E '(node_modules|\.git|dist|build|\.next|langflow|flowise|autogen|perplexica|litellm|external)')

if [ -n "$TEMP_DOCS" ]; then
    TEMP_COUNT=$(echo "$TEMP_DOCS" | wc -l | tr -d ' ')
    echo "  🧹 임시 문서 발견: ${TEMP_COUNT}개"
    echo "$TEMP_DOCS" | sed 's/^/     - /'
    echo "     권장: ./scripts/clean-temp-docs.sh 실행하여 정리"
    echo "     자동 정리: ./scripts/clean-temp-docs.sh --auto"
fi

echo "✅ Pre-commit checks completed"
exit 0

